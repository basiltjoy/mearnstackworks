for (let row = 1; row <= 1; row++) {
    let str = "*";
    for (let col = 1; col <= 1; col++) { //innerloop works first then second loop 
        col = str
    }
    console.log(str);
    for (let row = 1; row <= 1; row++) {
        let str = "**";
        for (let col = 1; col <= 1; col++) { //innerloop works first then second loop 
            col = str
        }
        console.log(str);
    }
    for (let row = 1; row <= 1; row++) {
        let str = "***";
        for (let col = 1; col <= 1; col++) { //innerloop works first then second loop 
            col = str
        }
        console.log(str);
    }
    for (let row = 1; row <= 1; row++) {
        let str = "****";
        for (let col = 1; col <= 1; col++) { //innerloop works first then second loop 
            col = str
        }
        console.log(str);
    }

}